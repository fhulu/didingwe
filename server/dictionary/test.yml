values_test:
  type: input_page
  inputs:
    - first_name
    - cookie
  values:
    - read_header: [cookie]


test_select:
  type: input_page
  inputs:
    - country: { type: dropdown   }

country:
  options:
    - let: { collection: country }
    - db.data: [collection, [collection], v0, v1]
